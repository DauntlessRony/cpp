#include <iostream>

using namespace std;

int main()
{
    cout << "9. Write a C program that accepts two integers from the user and calculate the sum of the two integers." << endl;
    int a,b;
    cout << "Input the first integer: ";
    cin >> a;
    cout << "Input the first integer: ";
    cin >> b;

    cout << "\nSum of the above two integers = "<< a+b << endl;

    return 0;
}
